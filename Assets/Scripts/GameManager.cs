using UnityEngine;

[CreateAssetMenu(fileName = "GameManager", menuName = "Scriptable Objects/GameManager")]
public class GameManager : ScriptableObject
{
    
}

// should have some scene tracker (switches scenes when the player selects a level theme
                                  // or when the player finishes a level)
// should have all the fields that are in a save file
    // should load the fields into their specific contexts

// menu pause bool

// player inventory
